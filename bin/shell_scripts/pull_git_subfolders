#!/bin/sh

for i in *; do 
  if [ -d "$i" ]; then 
    echo "------------------------------------------------------------------------------------------------------------"
    echo "+ Directory $i"; 
    cd "$i"; 
    git pull
    cd ..; 
  fi; 
done
